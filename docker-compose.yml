services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: japh-portfolio:latest
    container_name: japh-portfolio
    ports:
      - "3000:3000"
    env_file:
      - path: .env.local
        required: false
    restart: unless-stopped
